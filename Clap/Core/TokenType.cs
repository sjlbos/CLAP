namespace Clap.Core
{
    internal enum TokenType
    {
        ArgumentList,
        Flag,
        FlagList,
        Option,
        OptionList,
        PositionalArgument,
        PositionalArgumentList,
        KeyValuePair 
    }
}
